---
type Props = astroHTML.JSX.InputHTMLAttributes

const { class: className, ...rest } = Astro.props
---

<input
  class:list={[
    'focus:border-primary border-b-4 border-black transition-colors outline-none',
    className,
  ]}
  {...rest}
/>
